# Design Pattern

GoF(Gang of Four) 디자인 패턴을 Java로 학습한 예제 모음입니다.  
각 패턴별로 단계별(step) 또는 사례별(case) 예제 코드를 포함하고 있습니다.

---

## 📁 프로젝트 구조

| 폴더                    | 패턴     | 설명                                                         |
|-----------------------|--------|------------------------------------------------------------|
| `01.SingletonPattern` | 싱글톤    | 인스턴스를 하나만 생성해 전역에서 사용 (Database, LogWriter 등)              |
| `02.FlyweightPattern` | 플라이웨이트 | 공통 데이터를 공유해 메모리 사용을 줄이는 패턴                                 |
| `03.BuilderPattern`   | 빌더     | 복잡한 객체 생성 과정을 단계적으로 표현 (Person 빌더 등)                       |
| `04.ObserverPattern`  | 옵저버    | 상태 변경 시 구독자에게 알림 (Publisher/Observer, PlayController)      |
| `05.AdapterPattern`   | 어댑터    | 기존 인터페이스를 새 인터페이스에 맞게 감싸서 사용 (APlayer/BPlayer)             |
| `06.FacadePattern`    | 파사드    | 복잡한 서브시스템을 단순한 인터페이스로 제공 (HomeFacade, SimpleProductFacade) |
| `07.DecoratorPattern` | 데코레이터  | 객체에 동적으로 책임을 추가 (아이스크림 토핑, 학생 데코레이터)                       |
| `08.BridgePattern`    | 브릿지    | 추상화와 구현을 분리 (IRobot/IAction, RobotModel1/2)                |
| `09.StrategyPattern`  | 전략     | 알고리즘을 캡슐화해 교체 가능하게 (Database 전략, AObj/BInterface)          |

---

## 🛠 실행 환경

- **언어**: Java
- **IDE**: IntelliJ IDEA

각 패턴 폴더(예: `01.SingletonPattern`)는 독립적인 Java 프로젝트 구조이며,  
`src` 하위의 `TestPattern.java`(또는 `TestPattern1.java`, `TestPattern2.java`)의 `main` 메서드를 실행하면 됩니다.

---

## 📖 사용 방법

1. 저장소 클론
   ```bash
   git clone https://github.com/YOUR_USERNAME/DesignPattern.git
   cd DesignPattern
   ```
2. IntelliJ에서 **File → Open**으로 `DesignPattern` 폴더를 선택해 열기
3. 프로젝트가 9개 모듈(01.SingletonPattern ~ 09.StrategyPattern)로 열리면, 실행할 모듈의 `TestPattern*.java`에서 `main` 옆 ▶️ 버튼으로 실행

---

## 📄 라이선스

학습용 예제 프로젝트입니다.
